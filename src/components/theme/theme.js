import { createTheme } from "@mui/material";

const color = "#611f69/ #f6efe8/ #fff/ #1d1d1d";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1d1d1d",
    },
    secondary: {
      main: "#f6efe8",
    },
  },
});

export default theme;
